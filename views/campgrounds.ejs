<% include partials/header %>

<h1>Campgrounds page</h1>

<% campgrounds.forEach(function(cg) { %>
    <div>
        <h4><%= cg.name %></h4>
        <img src="<%= cg.image %>">
    </div>
<% }); %>

<% include partials/footer %>